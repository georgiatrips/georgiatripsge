export const metadata = {
  title: "Coupons | GeorgiaTrips",
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: {
      index: false,
      follow: false,
      noimageindex: true,
    },
  },
};

export default function CouponsLayout({ children }) {
  return children;
}
